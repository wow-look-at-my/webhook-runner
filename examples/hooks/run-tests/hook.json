{
  "description": "Run the test suite for an incoming push",
  "image": "golang:1.24-alpine",
  "command": ["sh", "-c", "echo 'tests would run here for run id $HOOK_RUN_ID'"],
  "workdir": "/workspace",
  "synchronous": true,
  "timeout": "15m"
}
